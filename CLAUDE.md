# Curtain 작업 규칙

이 파일은 사람과 에이전트가 같이 읽는 프로젝트 관례다. 설계 내용은
`docs/design.md`, 결정 근거는 `docs/adr/` 에 있다.

## 문서 참조는 번호가 아니라 이름으로

코드 주석에 **절 번호를 쓰지 않는다.** 문서는 계속 늘고 절 번호는 밀리므로,
번호로 가리킨 주석은 조용히 거짓이 된다. 실제로 초기 커밋에는 존재하지 않는
절을 가리키는 주석이 이미 섞여 있었다.

```python
# 나쁨
"""좌석 선점 — 설계 문서 §5.2 (원칙 02)"""

# 좋음
"""좌석 선점 — 설계 문서: 좌석 선점 (원칙 "부분 성공은 없다")"""
```

같은 이유로 원칙도 번호가 아니라 문장으로 부른다. 이름은 찾아보지 않아도
뜻이 읽히고, 문서가 재편되어도 grep 으로 찾을 수 있다.

결정 기록은 `결정 기록: 마이그레이션 전략` 처럼 주제로 가리킨다. 파일명의
숫자는 정렬용이며 주석에 옮겨 적지 않는다.

## 네 가지 원칙

1. **정합성의 단일 진실은 PostgreSQL이다.** Redis 는 전부 최적화 계층 —
   통째로 날려도 오버부킹은 발생하지 않아야 한다.
2. **부분 성공은 없다.** 4석 요청은 4석 전부이거나 0석이다.
3. **모든 상태 전이는 조건부 쓰기로만 한다.** `WHERE status = 기대값` 없는
   `UPDATE` 는 리뷰에서 거절한다.
4. **외부 호출은 리컨실러가 뒤를 받친다.** 결제가 "실패한 것"과 "응답을 못
   받은 것"은 다른 사건이다.

## 계층 규칙

`app/domain/` 은 DB 도 Redis 도 HTTP 도 모른다. 표준 라이브러리만 import 한다.
그래야 상태 전이 규칙과 정책 계산을 인프라 없이 테스트할 수 있다
(`make test-unit` 이 그것만 돌린다).

트랜잭션 경계는 `app/service/` 에 있다. 쿼리는 트랜잭션을 열지 않는다.

## 쿼리는 Core 표현식, 세션은 쓰지 않는다

쿼리는 `app/infra/db/queries.py` 의 SQLAlchemy Core 표현식이고, 상수가 아니라
값을 키워드 인자로 받는 함수다. 모델의 컬럼과 제약 이름을 직접 참조하므로
스키마와 어긋나면 import 시점에 깨진다.

ORM 세션은 쓰지 않는다. 모델(`app/infra/db/models.py`)은 스키마 정의,
마이그레이션 diff, 그리고 쿼리가 참조할 컬럼 이름표까지가 역할이다.

두 가지는 반드시 지킨다 (근거는 결정 기록: 쿼리 작성 방식).

- **좌석 상태는 리터럴로 박는다.** 바인드 파라미터로 넘기면 부분 인덱스
  `ix_hold_expiry` 가 매칭되지 않고 Seq Scan 이 된다. 실측했다.
- **잠금 절은 `tests/test_queries.py` 가 지킨다.** `FOR UPDATE`,
  `SKIP LOCKED`, `guard` 절이 사라지는 변경은 반드시 빨개져야 한다.
  새 쿼리를 추가하면 `_every_statement()` 목록에도 넣는다.

## 마이그레이션

`alembic revision --autogenerate` 로 뽑고 **반드시 읽는다.** ENUM 값 추가와
부분 인덱스 조건절 변경은 autogenerate 가 잡지 못한다. `make drift` 로
모델과 DB 가 어긋났는지 확인한다.

## 동시성 주장에는 테스트를 붙인다

"이러면 안전하다"는 주석은 근거가 아니다. `tests/test_concurrency.py` 에
실제로 동시 요청을 던져 확인하고, 통과 로그가 근거가 된다. 구매 한도가
동시 요청에서 새는 것도 그렇게 발견했다.
